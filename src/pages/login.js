import LoginForm from "@/components/LoginForm/LoginForm"; // Import component LoginForm
import { useEffect } from "react";
import { useToken } from "@/context/TokenProvider";
export default function LoginPage() {
  
  
  return <LoginForm />;
}
